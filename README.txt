Server Side Notlar
    1. Projede DB için docker üzerinde mongo ayağı kaldırıdm.
    2. Projeyi express-generator ile oluşturdum
    3. CORS problemli için cors import ettim
    4. Projeyi index route olarak küçükttüm ve oradan manage ettim.
    5. Loglama için morgan'ı kullandım
    6. Dataları server to client yaparken json olarak render ettirttim.  


Client Side Notlar

    1. Genelde component isimlerini doğrudan klasor adı olarak verdim ve içerisinde hepsini index.vue olarak belirledim. Okunabilirlik olakara 
    bazen kafam karıştığını söylebilirim.
    2. Route işlemleri için route klasörü açtım ve onu main.js'e import ettim bunun yanında yönlendirmede de navbarı kullandım
    3. Kod okunabilirği için method'lara yorum satırı ekledim.
    4. Projeti doğrudan vue create (default) olarak seçtim
    5. Kullandığım yapılar
        a. Component import etme
        b. Custom directives
        c. Route yapısı
        d. Axıos ile request atma


